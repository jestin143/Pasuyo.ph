// Customer-related backend/API functions only
import { supabase } from '../../lib/supabase/client.js'

export const customerService = {
  // createCustomer, getCustomer, updateProfile → to be implemented
}
