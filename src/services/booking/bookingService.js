// Booking/pasabuy-related functions only
import { supabase } from '../../lib/supabase/client.js'

export const bookingService = {
  // createBooking, getBookings, assignRider, updateStatus → to be implemented
}
